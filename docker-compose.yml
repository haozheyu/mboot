services:
  netboot:
    image: xxxx/xxxx/xxxx:xxxx
    container_name: myBoot
    restart: unless-stopped
    network_mode: host
    command: ["--data-dir", "/data", "--host", "0.0.0.0", "--port", "8088", "--no-browser"]
    volumes:
      - ./data:/data
